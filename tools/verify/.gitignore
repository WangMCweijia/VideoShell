# tools/verify/ 是**正式回归资产**（CI 每次构建都跑），但仓库根的 .gitignore 把 `_*`
# 一律当成"临时文件"（`_*.py` / `_*.html` / `_bs/` …）。子目录的规则优先，所以在这里
# 用「**先全忽略、再逐项放行**」把它们救回来 —— 顺带天然挡住了 harness 每次重跑生成的
# 编译产物与报告（`_bs4/`、`_seedcls/`、`_family_out/`、`_*.out.txt`、`_fail_*.txt` …）。
_*

# 路径解析中心：全部 harness 都靠它，缺它整套跑不起来
!_cp.py

# 真实夹具 —— 离线断言读的就是这些文件，它们不是"临时文件"
!_shell/
!_shell31/
!_live31/
!_play/
!_yg/
!_ygchunks/
!_ygcoverlive/
!_ygdec/
!_ygjs/
!_ygmedia/
!_ygo/
!_zq/
!_zq29/
!_zq402d_js/
!_zqjs/
!_zqjs2/

# 夹具目录**内部**的文件也放行（仓库根的 `_*.txt` 会挡住 `_ygo/_recipe.txt`）。
# 输出目录本身不在白名单里 ⇒ 仍被忽略 ⇒ 里面的东西也不会被这条带进来。
!_*/*

# 顶层那些**抓下来的样本文件**（`_yg_home.html` / `_zqana_A.m3u8` …）：
# 仓库根用的是 `_*.html` / `_*.m3u8` 这种"按扩展名忽略"，所以它们会被挡掉，得单独放行。
!_*.html
!_*.m3u8

# 夹具目录里残留的探针输出（`_zqjs2/probe.out.txt` 之类）：没有任何断言读它，别带进仓库
_*/*.out.txt

# 每日探针的运行产物：`probe_daily.py` 每天写一份 `probe_<日期>.md`，并往 `history.tsv`
# 追加一行。这是给人看的巡检记录、天天在变，**不是回归资产** ⇒ 不入仓。
# 注意上面那条 `_*` 只挡**下划线开头**的，`probe/` 不在其列，必须单独写。
probe/
