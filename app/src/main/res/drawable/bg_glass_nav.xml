<?xml version="1.0" encoding="utf-8"?><!--
  底部 Dock 底（v1.0.63 · 亮色=白面板 / 暗色=墨岛）。

  两层：
    ① 主体   dock_fill → dock_fill_2，自上而下微差（保留浮岛时代那道"漫反射"残影）
              亮色 = 白 → 极浅暖（白面板从暖纸画布上抬起一档）
              暗色 = 墨 → 更深墨（黑场里再压暗会消失，故只提亮一档）
    ② 1px 描边 dock_stroke。**极性随主题反转**，这就是描边走 Token 而不是写死颜色的原因：
              暗色 = 8% 白    —— 黑场上只有白发丝线勾得出上棱
              亮色 = 浅墨 #D9D5CA —— 白面板上画白线等于没画

  圆角取 radius_xl（20dp）：Dock 高 64dp，20dp 已经很接近药丸形，方角立刻露出"贴边"的廉价感。

  ⚠️ 不要在这里加 <padding>：高度由 Dock 自己定（dock_h），塞 padding 会把图标行挤歪。

  v1.0.49→v1.0.63 演变：玻璃浮岛（半透明 + 砂质 + 棱）→ 实底白面板 → 墨岛 →
  亮色回白面板 / 暗色留墨岛。
  两次"白面板"的区别在**靠什么分层**：v1.0.61 那版只有 fill + stroke、
  又没有 elevation，所以观感只是"一块白"；现在白面板 + 浅墨发丝线 + 12dp 投影
  是三件套，边界由描边与投影承担 —— 不必再靠明度反转去制造对比，
  那样做在亮色主题里就是**主题反转**（用户一眼就否掉了）。
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item>
        <shape android:shape="rectangle">
            <corners android:radius="@dimen/radius_xl" />
            <gradient
                android:angle="270"
                android:endColor="@color/dock_fill_2"
                android:startColor="@color/dock_fill"
                android:type="linear" />
        </shape>
    </item>

    <item>
        <shape android:shape="rectangle">
            <corners android:radius="@dimen/radius_xl" />
            <solid android:color="#00000000" />
            <stroke
                android:width="@dimen/glass_stroke_w"
                android:color="@color/dock_stroke" />
        </shape>
    </item>
</layer-list>
