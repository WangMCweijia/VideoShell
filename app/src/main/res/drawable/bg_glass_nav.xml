<?xml version="1.0" encoding="utf-8"?><!--
  底部 Dock 底（v1.0.62 · 墨岛）。

  两层：
    ① 主体   dock_fill → dock_fill_2，自上而下微深（保留浮岛时代那道"漫反射"残影）
    ② 1px 描边 dock_stroke —— 亮色是 15% 白：深墨盘上只有白发丝线勾得出上棱，
              用旧的 glass_stroke（浅墨 #D9D5CA）会变成一圈突兀的亮边。

  圆角取 radius_xl（20dp）：Dock 高 64dp，20dp 已经很接近药丸形，方角立刻露出"贴边"的廉价感。

  ⚠️ 不要在这里加 <padding>：高度由 Dock 自己定（dock_h），塞 padding 会把图标行挤歪。

  v1.0.49→v1.0.62 的演变：玻璃浮岛（半透明 + 砂质 + 棱）→ 实底白面板 → 墨岛。
  亮色主题的白 Dock 压在暖纸画布上，怎么调都是"一块白"，这不是参数问题而是材质
  没有对比；墨岛把"画布 → Dock"的层次一次性拉满，这也解释了为什么之前两轮
  "调描边 / 加胶囊"用户都说质感没变。
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item>
        <shape android:shape="rectangle">
            <corners android:radius="@dimen/radius_xl" />
            <gradient
                android:angle="270"
                android:endColor="@color/dock_fill_2"
                android:startColor="@color/dock_fill"
                android:type="linear" />
        </shape>
    </item>

    <item>
        <shape android:shape="rectangle">
            <corners android:radius="@dimen/radius_xl" />
            <solid android:color="#00000000" />
            <stroke
                android:width="@dimen/glass_stroke_w"
                android:color="@color/dock_stroke" />
        </shape>
    </item>
</layer-list>
