<resources xmlns:tools="http://schemas.android.com/tools">

    <!--
      主题基类：亮/暗只差颜色资源（values/colors.xml 与 values-night/colors.xml），
      状态栏图标明暗由 values-v23 / values-night-v23 覆盖同名 style 决定 ——
      亮色底要深色图标（windowLightStatusBar=true），暗色底要浅色图标（false）。
      不这么拆的话，亮色模式下会出现「白底白字」的状态栏（用户实测反馈）。
    -->
    <style name="Theme.VideoShell.Base" parent="Theme.MaterialComponents.NoActionBar">
        <!-- UI 2.0「暗场 Spotlight」：主强调 = 聚光琥珀（唯一高饱和色）。
             brand 蓝降级为 info/链接色，仍以 @color/brand 字面引用出现在
             链接文字、进度之外的语义处 —— 主题属性不再指向它。 -->
        <item name="colorPrimary">@color/amber</item>
        <item name="colorPrimaryVariant">@color/amber_dark</item>
        <item name="colorOnPrimary">@color/amber_ink</item>
        <item name="colorSecondary">@color/amber</item>
        <item name="colorOnSecondary">@color/amber_ink</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnSurface">@color/text_primary</item>
        <item name="android:colorBackground">@color/bg</item>
        <item name="colorError">@color/bad</item>
        <item name="android:windowBackground">@color/bg</item>
        <item name="android:statusBarColor">@color/bg</item>
        <item name="android:navigationBarColor">@color/bg_deep</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
        <item name="android:textColorHint">@color/text_hint</item>
    </style>

    <style name="Theme.VideoShell" parent="Theme.VideoShell.Base" />

    <style name="Theme.VideoShell.Player" parent="Theme.VideoShell">
        <item name="android:windowBackground">@color/black</item>
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:navigationBarColor">@color/black</item>
    </style>

    <style name="TextTitle">
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:maxLines">1</item>
        <item name="android:ellipsize">end</item>
    </style>

    <style name="TextSub">
        <item name="android:textColor">@color/text_secondary</item>
        <item name="android:textSize">12sp</item>
        <item name="android:maxLines">1</item>
        <item name="android:ellipsize">end</item>
    </style>

    <style name="CardSurface">
        <item name="android:background">@drawable/bg_surface</item>
    </style>

    <!--
      搜索范围/动作类小圆片。选中态由 `bg_chip` 的 state_selected 给底色，
      文字颜色必须跟着换（深底 + 深字 = 看不见），所以走 @color/chip_text 这个 state list。
      不要用 android:textColor 直接写死品牌色：选中时品牌色底 + 品牌色字 = 一片糊。
    -->
    <style name="ChipSmall">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">30dp</item>
        <item name="android:background">@drawable/bg_chip</item>
        <item name="android:gravity">center</item>
        <item name="android:paddingStart">12dp</item>
        <item name="android:paddingEnd">12dp</item>
        <item name="android:textColor">@color/chip_text</item>
        <item name="android:textSize">12sp</item>
        <item name="android:maxLines">1</item>
    </style>

</resources>
